# Backend environment variables
# Request orchestrator to set these in the real .env as needed.

# Absolute path to SQLite DB file (shared with database container).
SQLITE_DB=/home/kavia/workspace/code-generation/bus-booking-system-243652-243667/bus_app_database/myapp.db

# Admin bearer token for MVP admin endpoints.
ADMIN_TOKEN=dev-admin-token
